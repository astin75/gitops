apiVersion: v1
kind: Service
metadata:
  name: visitor-backend-service
  namespace: dev
  labels:
    app: visitor-backend
    environment: dev
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: visitor-backend
    environment: dev
---
apiVersion: v1
kind: Service
metadata:
  name: visitor-frontend-service
  namespace: dev
  labels:
    app: visitor-frontend
    environment: dev
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30030
    protocol: TCP
    name: http
  selector:
    app: visitor-frontend
    environment: dev